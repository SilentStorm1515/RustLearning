//rust's primitive types include:
// --> integers: u8, i8, u16, i16, u32, i32, u64, i64, u128, i128 (u means "unsigned", number means amount of memory taken up)
// --> floats: f32, f64
// --> booleans: bool
// --> characters: char
// --> tuples
// --> arrays

pub fn run() {

}